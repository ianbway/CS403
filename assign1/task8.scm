; Ian Braudaway
; CS403 Assign 1, Task 8

(define (ecfr value)

	
	)

(define (ecfi value)

	
	)

(define (main)
	(setPort (open (getElement ScamArgs 1) 'read))
	(define arg (readExpr))
	(println (w arg))
	)